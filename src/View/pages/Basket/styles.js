import { StyleSheet } from "aphrodite";

export default StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "column",
    maxHeight: "100vh",
    height: "100%",
    overflow: "hidden",
  },
  content: {
    height: "100%",
    overflow: "auto",
  },
});
