import { StyleSheet } from "aphrodite";
import oda from "../../../../common/theme/oda";

export default StyleSheet.create({
  radioGroupContainerFlex: {
    display: "flex",
    flexDirection: "column",
    gap: "8px",
  },
});
