import * as selectedTabActions from './actions';
import * as selectedTabSelectors from './selectors';
import reducer from './reducers';

export { selectedTabActions };

export { selectedTabSelectors };

export default reducer;
